### POST /api/chat

**Request:**
```json
{
  "messages": [
    { "role": "user", "content": "Hello, who are you?" }
  ]
}
### Response:

{
  "id": "...",
  "object": "chat.completion",
  ...
}

---

# **Готово!**

**Теперь можете отправлять ваш микросервис на отдельный сервер:**
- Убедитесь, что `.env` присутствует (секреты — только там!)
- Используйте systemd/Docker/pm2 для запуска.
- Позже сервис легко масштабируется за счёт горизонтального масштабирования.

---

**При необходимости расширения — добавляйте новые роуты, сервисы, middleware, следуя такой же структурной схеме!**
